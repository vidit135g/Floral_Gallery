<?xml version="1.0" encoding="utf-8"?>
<com.absolute.floral.ui.widget.SquareFrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="1.5dp">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardCornerRadius="6dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#1F000000"
        android:foreground="?selectableItemBackground">

        <ImageView
            android:id="@+id/image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/photo"
            android:scaleType="centerCrop" />

        <TextView
            android:id="@+id/video_dur"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="4dp"
            android:paddingHorizontal="5dp"
            android:paddingVertical="1dp"
            android:textColor="#FFFFFFFF"
            android:textSize="10sp"
            android:shadowColor="#80000000"
            android:shadowRadius="4"
            android:visibility="gone" />

        <View
            android:id="@+id/sel_scrim"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#8034C7B0"
            android:visibility="gone" />
    </androidx.cardview.widget.CardView>
</com.absolute.floral.ui.widget.SquareFrameLayout>
